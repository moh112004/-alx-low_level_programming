#include <stdio.h>

/**
* main - prints program name
*@argc: the number of arguments
*@argv: an array contents arguments
*Return: returns 0
*/
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
