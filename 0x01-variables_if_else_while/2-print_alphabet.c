#include <stdio.h>

/**
 * main - prints the alphabet in lowercase,
 * after that prints new line
 * Return: always 0
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; ch++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
