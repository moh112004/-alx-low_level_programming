#include <stdio.h>

/**
 * main - prints single digits between 0 and 10 
 * after that prints new line
 * Return: always 0
 */
int main(void)
{
int a;

for(a = 0;a<10;a++)
{
printf("%d",a);
}
return (0);
}
